---

import {format as formatDate} from 'date-fns';

interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={formatDate(date, 'MMMM dd, yyyy')}>
	{
		formatDate(date, 'MMMM dd, yyyy')
	}
</time>
